# Detekt configuration (1.23.8)

build:
  maxIssues: 0
  excludeCorrectable: false

config:
  validation: true
  warningsAsErrors: false
  checkExhaustiveness: false

# ---- Style rules ----
style:
  active: true

  # NFC encoding, colors, protocol constants, UI padding — all intentional
  MagicNumber:
    active: false

  WildcardImport:
    active: true
    excludeImports:
      - 'java.util.*'
      - 'androidx.compose.*'
      - 'androidx.room.*'
      - 'kotlinx.coroutines.*'

  MaxLineLength:
    active: true
    maxLineLength: 140
    excludePackageStatements: true
    excludeImportStatements: true

  MayBeConst:
    active: true

  UnusedPrivateMember:
    active: true
    allowedNames: ''

  UnusedPrivateProperty:
    active: true
    allowedNames: '_|ignored|expected'

  UnusedParameter:
    active: true
    allowedNames: '_.*|ignored|expected'

  ReturnCount:
    active: true
    max: 3
    excludedFunctions:
      - 'equals'
    excludeReturnFromLambda: true

  LoopWithTooManyJumpStatements:
    active: true
    maxJumpCount: 1

  ForbiddenComment:
    active: false

  UseRequire:
    active: true

# ---- Complexity rules ----
complexity:
  active: true

  LongParameterList:
    active: true
    functionThreshold: 6
    constructorThreshold: 6
    ignoreDefaultParameters: true
    ignoreDataClasses: true

  TooManyFunctions:
    active: true
    thresholdInFiles: 150
    thresholdInClasses: 60
    thresholdInInterfaces: 20
    thresholdInObjects: 55
    thresholdInEnums: 15
    ignoreOverridden: true

  LongMethod:
    active: true
    threshold: 60

  CyclomaticComplexMethod:
    active: true
    threshold: 15

  NestedBlockDepth:
    active: true
    threshold: 4

# ---- Potential bugs ----
potential-bugs:
  active: true

  IgnoredReturnValue:
    active: true

  NullableToStringCall:
    active: false

  ImplicitDefaultLocale:
    active: true

# ---- Exceptions ----
exceptions:
  active: true

  TooGenericExceptionCaught:
    active: true

# ---- Performance ----
performance:
  active: true

  SpreadOperator:
    active: true

# ---- Naming ----
naming:
  active: true

  FunctionNaming:
    active: true
    # Allow PascalCase for Composable functions (standard Compose convention)
    functionPattern: '[a-zA-Z][a-zA-Z0-9]*'
    excludeClassPattern: '$^'

  FunctionParameterNaming:
    active: true
    parameterPattern: '(_)?[a-z][A-Za-z0-9]*'
    excludeClassPattern: '$^'

  TopLevelPropertyNaming:
    active: true
    constantPattern: '[A-Z][_A-Z0-9]*'
    propertyPattern: '[A-Za-z][_A-Za-z0-9]*'
    privatePropertyPattern: '_?[A-Za-z][_A-Za-z0-9]*'

  VariableNaming:
    active: true
    variablePattern: '[a-z][A-Za-z0-9]*'
    privateVariablePattern: '(_)?[a-z][A-Za-z0-9]*'
    excludeClassPattern: '$^'

# ---- Coroutines ----
coroutines:
  active: true

  InjectDispatcher:
    active: true

  RedundantSuspendModifier:
    active: true

  SuspendFunWithFlowReturnType:
    active: true
